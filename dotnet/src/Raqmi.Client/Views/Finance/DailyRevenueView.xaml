<UserControl x:Class="Raqmi.Client.Views.Finance.DailyRevenueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="0,0,8,8">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Border Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,12">
      <StackPanel>
        <TextBlock Text="Nouvelle recette" Style="{StaticResource LabelText}"/>
        <Grid Margin="0,8,0,0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <StackPanel Grid.Column="0" Margin="0,0,8,0">
            <TextBlock Text="Montant" Style="{StaticResource LabelText}"/>
            <TextBox x:Name="AmountBox" Text="0"/>
          </StackPanel>
          <StackPanel Grid.Column="1" Margin="0,0,8,0">
            <TextBlock Text="Cat?gorie" Style="{StaticResource LabelText}"/>
            <TextBox x:Name="CategoryBox" Text="general"/>
          </StackPanel>
          <StackPanel Grid.Column="2" Margin="0,0,8,0">
            <TextBlock Text="Notes" Style="{StaticResource LabelText}"/>
            <TextBox x:Name="NotesBox"/>
          </StackPanel>
          <Button Grid.Column="3" x:Name="AddButton" Content="Ajouter" Style="{StaticResource PrimaryButton}"
                  VerticalAlignment="Bottom" Click="OnAdd"/>
        </Grid>
        <TextBlock x:Name="ErrorText" Foreground="#FCA5A5" Margin="0,8,0,0" Visibility="Collapsed"/>
      </StackPanel>
    </Border>
    <DataGrid x:Name="Grid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Date" Binding="{Binding BusinessDate}" Width="*"/>
        <DataGridTextColumn Header="Montant" Binding="{Binding Amount}" Width="100"/>
        <DataGridTextColumn Header="Cat?gorie" Binding="{Binding Category}" Width="*"/>
        <DataGridTextColumn Header="Statut" Binding="{Binding Status}" Width="100"/>
        <DataGridTemplateColumn Header="Action" Width="100">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <Button Content="Valider" Style="{StaticResource GhostButton}" Click="OnValidate" Tag="{Binding Id}"/>
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
      </DataGrid.Columns>
    </DataGrid>
  </Grid>
</UserControl>
