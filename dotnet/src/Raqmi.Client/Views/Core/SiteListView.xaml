<UserControl x:Class="Raqmi.Client.Views.Core.SiteListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="0,0,8,8">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Border Grid.Row="0" Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,12">
      <StackPanel>
        <TextBlock Text="Nouveau site" Style="{StaticResource LabelText}"/>
        <Grid Margin="0,8,0,0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <StackPanel Grid.Column="0" Margin="0,0,8,0">
            <TextBlock Text="Code" Style="{StaticResource LabelText}"/>
            <TextBox x:Name="CodeBox"/>
          </StackPanel>
          <StackPanel Grid.Column="1" Margin="0,0,8,0">
            <TextBlock Text="Nom" Style="{StaticResource LabelText}"/>
            <TextBox x:Name="NameBox"/>
          </StackPanel>
          <StackPanel Grid.Column="2" Margin="0,0,8,0">
            <TextBlock Text="Type" Style="{StaticResource LabelText}"/>
            <ComboBox x:Name="TypeBox" Margin="0,4,0,0"/>
          </StackPanel>
          <StackPanel Grid.Column="3" Margin="0,0,8,0">
            <TextBlock Text="Ville" Style="{StaticResource LabelText}"/>
            <TextBox x:Name="CityBox"/>
          </StackPanel>
          <Button Grid.Column="4" Content="Ajouter" Style="{StaticResource PrimaryButton}" VerticalAlignment="Bottom" Click="OnAdd"/>
        </Grid>
        <TextBlock x:Name="ErrorText" Foreground="#FCA5A5" Margin="0,8,0,0" Visibility="Collapsed"/>
      </StackPanel>
    </Border>
    <Border Grid.Row="1" Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,12" Visibility="Collapsed" x:Name="EditPanel">
      <StackPanel>
        <TextBlock x:Name="EditTitle" Style="{StaticResource LabelText}" Margin="0,0,0,8"/>
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>
          <StackPanel Grid.Column="0" Margin="0,0,8,0">
            <TextBlock Text="Nom" Style="{StaticResource LabelText}"/>
            <TextBox x:Name="EditNameBox"/>
          </StackPanel>
          <StackPanel Grid.Column="1" Margin="0,0,8,0">
            <TextBlock Text="Type" Style="{StaticResource LabelText}"/>
            <ComboBox x:Name="EditTypeBox" Margin="0,4,0,0"/>
          </StackPanel>
          <StackPanel Grid.Column="2" Margin="0,0,8,0">
            <TextBlock Text="Ville" Style="{StaticResource LabelText}"/>
            <TextBox x:Name="EditCityBox"/>
          </StackPanel>
        </Grid>
        <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
          <Button Content="Enregistrer" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" Click="OnSaveEdit"/>
          <Button Content="Annuler" Style="{StaticResource GhostButton}" Click="OnCancelEdit"/>
        </StackPanel>
      </StackPanel>
    </Border>
    <DataGrid x:Name="Grid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="100"/>
        <DataGridTextColumn Header="Nom" Binding="{Binding Name}" Width="*"/>
        <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
        <DataGridTextColumn Header="Ville" Binding="{Binding City}" Width="*"/>
        <DataGridTextColumn Header="Statut" Binding="{Binding StatusLabel}" Width="80"/>
        <DataGridTemplateColumn Header="Action" Width="180">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <StackPanel Orientation="Horizontal">
                <Button Content="Modifier" Style="{StaticResource GhostButton}" Margin="0,0,4,0" Click="OnEdit" Tag="{Binding Id}"/>
                <Button Content="Activer/Désactiver" Style="{StaticResource GhostButton}" Click="OnToggleActive" Tag="{Binding Id}"/>
              </StackPanel>
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
      </DataGrid.Columns>
    </DataGrid>
  </Grid>
</UserControl>
